<%= simple_form_for @service, url: service_path do |f| %>
  <%= f.input :title %>
  <%= f.input :session %>
  <%= f.input :detail %>
  <%= f.input :note %>
  <%= f.submit 'Submit', class: 'btn btn-primary' %>
<% end %>
